<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="24dp"
    android:background="@drawable/item_schedule_participant_selected_bg_shape"
    android:paddingStart="@dimen/dp_6"
    android:paddingEnd="@dimen/dp_6">

    <TextView
        android:id="@+id/tv_participant"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/dp_24"
        android:gravity="center_vertical"
        android:text="某"
        android:textColor="#ff333333"
        android:textSize="@dimen/text_14"
        android:singleLine="true"
        android:maxEms="5"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_del"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/dp_8"
        android:src="@drawable/schedule_label_delete_icon"
        app:layout_constraintBottom_toBottomOf="@id/tv_participant"
        app:layout_constraintLeft_toRightOf="@id/tv_participant"
        app:layout_constraintTop_toTopOf="@id/tv_participant"
        />
</androidx.constraintlayout.widget.ConstraintLayout>